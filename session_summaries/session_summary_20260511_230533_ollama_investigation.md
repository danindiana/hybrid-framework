# Session Summary: Ollama Usage Investigation
**Date:** Monday, May 11, 2026
**Timestamp:** 20260511_230533

## Objective
Investigate which program is currently attached to or using the Ollama models on the system.

## Findings

### 1. Ollama Service Architecture
The system is configured with three distinct Ollama instances to manage dual GPUs (RTX 3090 and RTX 3080):
- **Primary (`ollama.service`):** Port `11434` (RTX 3090, GPU 0).
- **Secondary (`ollama-secondary.service`):** Port `11435` (RTX 3080, GPU 1).
- **Coder (`ollama-coder.service`):** Port `11436` (RTX 3080, GPU 1, User: `jeb`).

### 2. Currently Loaded Models (Port 11434)
As of 23:05, the following models are loaded in memory on the primary instance:
- `nomic-embed-text:latest` (595 MB, 100% GPU)
- `qwen2.5-coder:14b` (18 GB, 15% CPU / 85% GPU)

The models are set with a `KEEP_ALIVE` of 60 minutes.

### 3. Active Program Identification
The investigation identified the **Gemini CLI** as the most likely program interacting with Ollama:
- **PID 3414684:** A `gemini` node process started at **23:01**.
- **Log Evidence:** The Ollama service logs recorded a burst of `POST /api/embeddings` requests from `127.0.0.1` at **23:01:44**, immediately following the start of the Gemini session.
- **Nginx Proxy:** Nginx is configured to proxy `http://localhost/` to `http://127.0.0.1:11434`, allowing programs to access Ollama via standard HTTP ports.

### 4. Other Background Services (Failing)
Several other agentic services are configured but currently in a failing state (`status=203/EXEC`):
- `gryphgen-agentic.service`: Failing to find python executable in venv.
- `agent-os-daemon.service` (User): Failing to find rust binary.
- `ollama-proxy.service`: Failing to find python executable.

## Conclusion
The current Ollama activity (specifically embeddings for the `nomic-embed-text` model) is directly correlated with the active **Gemini CLI** session (PID 3414684). Other potential candidates like `open-webui` are currently stopped.

---
*Generated by Gemini CLI*
