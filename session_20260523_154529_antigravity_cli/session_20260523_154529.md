# Session Document: Antigravity CLI Investigation
**Date:** 2026-05-23
**Timestamp:** 15:45:29

## Objective
Explain the "antigravity cli" and generate relevant documentation.

## Findings
The `antigravity` command was located at `/usr/bin/antigravity`. It is a customized CLI tool (v1.107.0) that shares many parameters with VS Code but includes specific enhancements for AI workflows:
- **MCP Support:** Commands for adding Model Context Protocol servers.
- **Chat Subcommand:** Direct CLI access to AI chat sessions.
- **Rebranding:** The package description is "Experience liftoff."
- **Performance Constraints:** The workspace contains a massive `archive/` directory with hundreds of thousands of files (including legacy `rheinmetall` project data). The Antigravity CLI, using standard VS Code watchers, struggles and gets "clogged" when indexing these.

## Actions Taken
1. Researched the `antigravity` command using `grep_search` and `which`.
2. Verified the CLI capabilities via `antigravity --help`.
3. Created a dedicated session folder: `session_20260523_154529_antigravity_cli`.
4. Generated a comprehensive `README.md` for the CLI.
5. Created this session document.
6. **Chat Testing:** Executed a test prompt (`antigravity chat "Hello"`) to verify the language server and AI daemon connectivity.
7. **Workspace Optimization:** Designed a `.vscode/settings.json` configuration to explicitly exclude `archive/`, `logs/`, and other massive directories from Antigravity's file watchers and search indexes.

## Next Steps
- Apply the `.vscode/settings.json` exclusions to restore workspace performance.
- Test MCP integration with local servers.
